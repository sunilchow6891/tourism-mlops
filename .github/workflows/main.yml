yaml_content = """
name: Tourism Project Pipeline

on:
  push:
    branches:
      - main

jobs:
  mlops-pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: |
          pip install pandas scikit-learn huggingface_hub joblib streamlit

      - name: Run Pipeline Scripts
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          # This triggers the scripts we wrote in Steps 1-4
          # Note: In a real repo, you would save those code blocks as .py files
          echo "Automating Model Training and Deployment..."
"""

with open("tourism_project/.github/workflows/pipeline.yml", "w") as f:
    f.write(yaml_content)
    
print("pipeline.yml has been created successfully.")
